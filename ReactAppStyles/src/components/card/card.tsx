//import "./styles.css"
//import styles from "./card.module.scss"

const Card = () =>{

    const textStyles: React.CSSProperties = {
        color : "blue",
        fontSize: "2rem",
        textAlign:"center"
    }

    return(
        <div style={textStyles}>
            esto es una card LOL
        </div>
    )

}

export default Card;